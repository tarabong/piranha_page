import { Box } from '@mui/material';

export const DiscographyPage = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh',
        gap: 2,
      }}
    >
      COMMING SOON
    </Box>
  );
};
